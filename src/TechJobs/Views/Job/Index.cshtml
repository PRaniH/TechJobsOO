@model TechJobs.ViewModels.SearchJobsViewModel

<h1>@Model.Title</h1>

@{
    // TODO #2.1- declare the ViewModel (outside of this @{ } block)
    //-Or JobFieldsViewModel-->
}

<table class="job-listing">
    @{
        // TODO #2.2 - display the job fields, one per row (outside of this @{ } block)
    }

    @foreach (var job in Model.Jobs){//Could probably get rid of this and update belows to Model.Jobs.Name etc
        <table class="job-listing">
            <tr>
                <td>Name</td>
                <td>@job.Name</td>
            </tr>
            <tr>
                <td>Employer</td>
                <td>@job.Employer</td>
            </tr>
            <tr>
                <td>Location</td>
                <td>@job.Location</td>
            </tr>
            <tr>
                <td>Skill</td>
                <td>@job.CoreCompetency</td>
            </tr>
            <tr>
                <td>Position Type</td>
                <td>@job.PositionType</td>

            </tr>
        </table>
    }
</table>